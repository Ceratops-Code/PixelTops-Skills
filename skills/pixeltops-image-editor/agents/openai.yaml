interface:
  display_name: "PixelTops Image Editor"
  short_description: "Precise mask-bounded editing, resizing, and pixel verification"
  default_prompt: "Use $pixeltops-image-editor for precise raster editing; constrain masked operations to an explicit mask and verify protected pixels."
